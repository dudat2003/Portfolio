# Example environment variables for this project.
# Copy this file to `.env.local` (or `.env.production`) and fill with real values.
# Do NOT commit secrets. Keep real keys out of source control.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Resend (email) API key used by server-side API at src/app/api/send/route.ts
RESEND_API_KEY=

# WebSocket server URL used by the client socket context (src/contexts/socketio.tsx).
# Example: http://localhost:4000 or https://ws.example.com
NEXT_PUBLIC_WS_URL=


NEXT_PUBLIC_CV_URL=
# Umami analytics script URL and site id used in src/app/layout.tsx
# UMAMI_DOMAIN should be the script src (e.g. https://umami.example.com/umami.js)
UMAMI_DOMAIN=
UMAMI_SITE_ID=

# Optional: any additional environment variables can be added here.